# 安全政策

## 报告漏洞

如果你发现安全漏洞，请**不要**通过公开 Issue 报告。

请通过以下方式联系我们：

1. 在 GitHub 上使用 "Security" 标签创建私密 Issue
2. 或发送邮件至项目维护者

我们会尽快回复并处理。

## 安全设计

本项目在设计上考虑了以下安全边界：

- **路径隔离**：图片只能从 `VISION_ASSET_ROOT` 沙箱目录读取
- **输入校验**：使用 Zod 严格校验所有 API 输出
- **文件限制**：限制文件大小和最大像素数
- **元数据清除**：导入时重编码图片，去除 EXIF 等元数据
- **不可信数据**：图片中的文字被视为不可信数据，不会作为 Agent 指令

## 依赖安全

定期运行 `npm audit` 检查依赖漏洞。
